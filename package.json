{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "dev:api": "cross-env PORT=5000 pnpm --filter @workspace/api run dev",
    "dev:web": "cross-env PORT=3000 VITE_API_URL=http://localhost:5000 BASE_PATH=/ pnpm --filter @workspace/web run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0"
  },
  "packageManager": "pnpm@10.32.1"
}
